# Скопіюй цей файл у .env.local і встав свій ключ.
# .env.local у .gitignore — у репозиторій НЕ потрапляє.
# Ключ використовується ТІЛЬКИ на сервері (Route Handler), у браузер не передається.
ANTHROPIC_API_KEY=
